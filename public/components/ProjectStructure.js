import React from 'react';

const ProjectStructure = () => {
  return (
    <div>
      <h2>Project Structure</h2>
      <p>The project is organized into the following key components:</p>
      <ul>
        <li>/client: Frontend code for the NexusNudge Marketing dashboard.</li>
        <li>/server: Backend API built with Express.js.</li>
        <li>/database: Database schema and migration scripts.</li>
        <li>/ml: Integration with machine learning models for personalized insights.</li>
      </ul>
    </div>
  );
};

export default ProjectStructure;
